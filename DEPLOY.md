# Инструкция по деплою проекта

Этот проект можно задеплоить бесплатно на нескольких платформах. Ниже приведены пошаговые инструкции для каждой платформы.

## 🚀 Вариант 1: Vercel (Рекомендуется)

Vercel отлично подходит для React + Vite проектов и предлагает бесплатный план с автоматическим деплоем.

### Шаги:

1. **Зарегистрируйтесь на [Vercel](https://vercel.com)** (можно через GitHub)

2. **Подключите репозиторий:**
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - Vercel автоматически определит настройки

3. **Настройки сборки (если нужно):**
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`

4. **Нажмите Deploy** - проект будет задеплоен автоматически!

5. **После деплоя** вы получите URL вида: `https://your-project.vercel.app`

### Преимущества:
- ✅ Автоматический деплой при push в GitHub
- ✅ Бесплатный SSL сертификат
- ✅ Быстрая CDN
- ✅ Простота настройки

---

## 🌐 Вариант 2: Netlify

Netlify также отлично работает с Vite проектами.

### Шаги:

1. **Зарегистрируйтесь на [Netlify](https://www.netlify.com)** (можно через GitHub)

2. **Подключите репозиторий:**
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите ваш GitHub репозиторий

3. **Настройки сборки:**
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`

4. **Нажмите "Deploy site"**

5. **Создайте файл `netlify.toml`** (опционально, для дополнительных настроек):
   ```toml
   [build]
     command = "npm run build"
     publish = "dist"

   [[redirects]]
     from = "/*"
     to = "/index.html"
     status = 200
   ```

### Преимущества:
- ✅ Автоматический деплой
- ✅ Бесплатный SSL
- ✅ Простая настройка

---

## ⚡ Вариант 3: Cloudflare Pages

Cloudflare Pages предлагает быстрый и бесплатный хостинг.

### Шаги:

1. **Зарегистрируйтесь на [Cloudflare](https://pages.cloudflare.com)**

2. **Подключите репозиторий:**
   - Нажмите "Create a project"
   - Выберите ваш GitHub репозиторий

3. **Настройки сборки:**
   - **Framework preset:** Vite
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`

4. **Нажмите "Save and Deploy"**

### Преимущества:
- ✅ Очень быстрая CDN
- ✅ Бесплатный SSL
- ✅ Неограниченная пропускная способность

---

## 📦 Вариант 4: GitHub Pages

GitHub Pages бесплатен, но требует немного больше настройки.

### Шаги:

1. **Установите пакет для деплоя:**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **Добавьте скрипты в `package.json`:**
   ```json
   "scripts": {
     "predeploy": "npm run build",
     "deploy": "gh-pages -d dist"
   }
   ```

3. **Обновите `vite.config.ts`** (добавьте `base`):
   ```typescript
   export default defineConfig({
     base: '/your-repo-name/', // замените на имя вашего репозитория
     // ... остальные настройки
   })
   ```

4. **Задеплойте:**
   ```bash
   npm run deploy
   ```

5. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - Выберите источник: `gh-pages` branch
   - Сохраните

### Преимущества:
- ✅ Полностью бесплатно
- ✅ Интеграция с GitHub

---

## 🔧 Общие настройки для всех платформ

### Если используете React Router:

Для правильной работы роутинга на всех платформах, убедитесь, что настроены редиректы для SPA (Single Page Application). Большинство платформ делают это автоматически, но если нужно:

**Для Netlify** - создайте файл `public/_redirects`:
```
/*    /index.html   200
```

**Для Vercel** - создайте файл `vercel.json`:
```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

---

## 📝 Рекомендации

1. **Для начала:** Используйте **Vercel** - это самый простой и быстрый способ
2. **Для продакшена:** Все три платформы (Vercel, Netlify, Cloudflare) отлично подходят
3. **Для простоты:** Vercel требует минимум настроек
4. **Для скорости:** Cloudflare Pages может быть быстрее

---

## 🎯 Быстрый старт с Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите репозиторий
5. Нажмите "Deploy"
6. Готово! 🎉

Ваш сайт будет доступен по адресу: `https://your-project.vercel.app`

